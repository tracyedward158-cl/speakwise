# SpeakWise 微信小程序化方案

> **状态**：P1 + P4 已落地（工程在 `mp/`），其余阶段待做 —— 见文末「实施进度」
> **编写日期**：2026-09-21
> **对应代码版本**：`d900fe6`（修复自定义练习）
> **文档目的**：在小程序开发启动前，把平台硬约束、主体选择、改造范围和工作量固定下来，避免开发到一半才发现某能力根本申请不到。

---

## 一、结论摘要

1. **代码本身可移植性良好**：`src/` 共 7103 行，其中约 40%（`data/` 全部、`utils/` 的多半）可原样复用，真正的重写量集中在语音链路和雷达图两处。
2. **四个平台硬约束决定成败**，且都不在代码层面：**语音能力**、**AI 生成内容合规**、**域名备案**、**资源体积**。前三项是外部依赖，应立刻并行启动。
3. **主体类型是所有决策的前提**。个人主体用不了「微信同声传译」插件、申不到「深度合成」类目，直接冲击本项目的两大核心功能（语音输入、AI 对话/评分）。**本方案默认按「个体工商户」主体规划**，个人主体作为科研演示场景的降级路线另行说明。
4. **推荐新建独立 Taro 工程，而非就地改造**。现有 Web 版正在承担数据收集职责，就地改造会中断研究；独立工程可通过共享纯逻辑模块做到"一套核心、两个端"。
5. **估算**：代码工作 4–5 周（单人），外部流程（备案 1–3 周 + 类目审核 + 算法备案材料获取约 10 天）需并行推进，**总周期取决于外部流程而非编码**。

---

## 二、现状盘点

### 2.1 技术栈

| 层 | 现状 | 小程序下 |
|---|---|---|
| 框架 | React 18 + Vite 6 | 需转 Taro |
| 路由 | react-router-dom 7（HashRouter），24 条路由 | 换 Taro 路由 + `app.json` |
| 后端 | Express（`server.cjs` + `server/`），部署为腾讯云 SCF Web Function | **不变** |
| 数据库 | TDSQL-C Serverless (MySQL) | **不变** |
| 认证 | JWT + bcryptjs，账号密码登录 | **不变**（可选增加微信登录） |
| AI | DeepSeek（`api.deepseek.com`，服务端代理） | **不变** |
| 语音评测 | 讯飞 ISE，服务端 WebSocket 转发 | **不变** |
| 语音识别 | 浏览器 `SpeechRecognition` | **必须重做** |
| 语音合成 | 浏览器 `speechSynthesis` | **必须重做** |
| 录音 | Web Audio API（16kHz PCM → WAV） | **必须重做** |
| 本地存储 | `localStorage`（35 处 / 9 文件） | 换 `wx.setStorageSync` |
| 网络 | `fetch` | 换 `Taro.request` |

### 2.2 代码规模

```
src/ 总计                       7103 行
├── pages/        18 个页面     3614 行
├── components/   13 个组件     1291 行
├── utils/        13 个模块     1739 行
├── context/       2 个         138 行
├── hooks/         3 个         121 行
└── data/          9 个         约 200 行 + 552 题题库
```

**静态资源体积是本项目的隐藏地雷**：

| 资源 | 体积 | 小程序限制 |
|---|---|---|
| `public/images/` | **49 MB**（11 张 PNG，单张 3.5–5.7 MB） | 主包 2 MB / 总包 20 MB（分包后 30 MB） |
| `public/logonew.png` | 456 KB | 同上 |
| `src/data/pronunciationBank.json` | 128 KB | 可直接进包 |

### 2.3 后端接口清单（**全部无需改动**）

```
POST   /api/chat                       DeepSeek 代理
POST   /api/evaluate                   讯飞 ISE 语音评测
GET    /api/health
POST   /api/auth/register
POST   /api/auth/login
GET    /api/auth/me
PATCH  /api/auth/me
POST   /api/auth/change-password
POST   /api/auth/class/join
POST   /api/auth/class/leave
POST   /api/records
POST   /api/records/migrate
GET    /api/records/mine
GET    /api/records/class              （教师）
GET    /api/records/scenarios
GET    /api/records/:id
POST   /api/tasks                      （教师）
GET    /api/tasks                      （教师）
GET    /api/tasks/mine
DELETE /api/tasks/:id                  （教师）
```

---

## 三、前置决策：主体类型

**这一节必须先定，因为它决定了后面哪些功能能做。**

### 3.1 三种主体的能力边界

| 能力 | 个人 | 个体工商户 | 企业 |
|---|---|---|---|
| 账号密码登录 / 自有后端 / 数据库 | ✅ | ✅ | ✅ |
| 录音 + 讯飞评测（`wx.getRecorderManager`） | ✅ | ✅ | ✅ |
| **微信同声传译插件（WechatSI）** | ❌ 类目不开放 | ✅ | ✅ |
| **深度合成类目**（AI 问答 / AI 创作） | ❌ 不开放 | ✅ | ✅ |
| `web-view` 组件 | ❌ 组件级写死 | ✅ | ✅ |
| 微信支付 | ❌ | ✅ | ✅ |
| 手机号快捷登录（`getPhoneNumber`） | ❌ | ✅ | ✅ |
| 微信认证 / 搜索权重 | ❌ | ✅ | ✅ |
| 直播组件 / 卡券 / 附近的小程序 | ❌ | 部分 | ✅ |
| ICP 备案 | ✅（教育类目多标注"不建议个人"） | ✅ | ✅ |
| 注册成本 | 免费 | 执照数百元、数日 | 较高 |

### 3.2 对本项目的直接冲击

- **语音识别与合成**：WechatSI 插件挂在 `IT科技 > 软件服务提供商` 类目下，**不在个人主体开放范围**。官方答复原文："不支持添加该插件，如有需要建议使用非个人主体。"
  → 影响 `useSpeech.js` 的全部能力，涉及 ChatView、DrillView、PronunciationDrill、ChatTranscript 四个调用点。
- **AI 生成内容**：AI 对话、AI 评分（`chatGrading.js`）、AI 注音翻译（`pronunciationBank.js` 的 `ENRICH_SYSTEM`）均属深度合成。个人主体未开放该类目；且有开发者反馈"添加插件/使用 AI 能力后无法通过审核"。

### 3.3 建议

**按个体工商户规划**，理由：

1. 个体户属非个人主体，上述被锁能力全部解锁；
2. 成本可控（数百元、数日办结）；
3. 微信开放社区对"深度合成类目为什么不可以是个人主体"的官方答复即为"你可以办个个体户的营业执照"。

**⚠️ 已知风险**：个体户能否作为**算法备案**主体，各方资料互相矛盾（微信端接受个体户申类目，但部分资料称算法备案仅限企业法人）。本项目走"第三方技术"路线（见 4.2）可绕开自办备案，但**落地前务必向当地通信管理局确认**。

---

## 四、四个平台硬约束

### 4.1 语音链路（最大的一块工作量）

现状：`useSpeech.js` 用 `SpeechRecognition`（识别）+ `speechSynthesis`（合成），`audioRecorder.js` 用 Web Audio API 采集 PCM 并封装 WAV。三者在任何小程序方案下都不存在。

#### 4.1.1 录音与评测 —— 好消息：服务端可零改动

现有 `/api/evaluate` 的入参处理逻辑（`server.cjs:276`）：

```js
const mp3Audio = audio.startsWith('UklGR') ? wavBase64ToMp3Base64(audio) : audio;
```

即：**base64 以 `UklGR`（"RIFF"）开头则走 WAV→MP3 转换，否则直接当作 MP3 使用**。

而 `wx.getRecorderManager()` 支持 `format: 'mp3'` 直接录制 MP3。因此：

```
小程序：wx.getRecorderManager({ format:'mp3', sampleRate:16000, numberOfChannels:1 })
   ↓ onStop 拿到 tempFilePath
   ↓ wx.getFileSystemManager().readFile({ encoding:'base64' })
   ↓ POST /api/evaluate  { audio: <mp3 base64>, refText, core }
服务端：isWAV 判断为 false → 跳过 lamejs 转换 → 直接送讯飞
```

**服务端一行都不用改。** 这是本方案中最省事的一环。

- **备选方案**（若 mp3 编码影响评测精度）：改用 `format:'PCM'` 采集裸 PCM，在小程序侧手工拼接 44 字节 WAV 头 —— `audioRecorder.js:15-46` 的 `buildWav` 逻辑可原样移植（小程序支持 `ArrayBuffer` / `DataView` / `Int16Array`）。代价是服务端要跑 lamejs 转码，CPU 开销上升。**建议先用 mp3 直传，实测分数异常再退回此方案。**

#### 4.1.2 语音识别 STT

| 方案 | 个人主体 | 个体户/企业 |
|---|---|---|
| WechatSI 插件 `getRecordRecognitionManager()` | ❌ | ✅ 推荐：免费、实时、免鉴权 |
| 服务端讯飞 IAT（语音听写） | ✅ | ✅ 备选 |

**推荐个体户走 WechatSI**，个人主体走服务端 IAT（`server.cjs` 已有讯飞 WebSocket 客户端，新增一个 IAT 方法即可，凭证复用）。

> ⚠️ **WechatSI 不提供任何变通手段**，且该限制**在开发者工具中不报错，往往提审才暴露** —— 这是本项目最容易踩的坑。

#### 4.1.3 语音合成 TTS

现状 `speak(text, slow)` 支持 `rate: 0.45` 慢速朗读，这是**教学场景的关键交互**（跟读示范）。

- WechatSI `textToSpeech()` 返回音频 URL，需 `wx.downloadFile` + `InnerAudioContext` 播放，**语速不可调**；
- 服务端讯飞在线合成（`tts`）同样以整段音频返回，语速在合成时指定，可支持慢速。

**结论**：慢速朗读需求决定了**必须走语速可配的合成方案**，WechatSI 的 TTS 无法满足。建议统一走**服务端讯飞在线合成**（与 IAT 同源，凭证复用），两端行为一致。这反过来也简化了实现 —— STT 和 TTS 走同一条服务端链路。

播放封装为常驻 `InnerAudioContext` 单例。现有 `ChatBubble.jsx` 顶部注释已指出"不要逐气泡调 useSpeech，`window.speechSynthesis` 是全局单例"—— 这个约束在 `InnerAudioContext` 下同样成立，迁移时保留该设计。

#### 4.1.4 麦克风权限

需在 `app.json` 声明 `scope.record` 权限，并处理用户拒绝后的引导（`wx.openSetting`）。现有代码无此逻辑，属新增。

### 4.2 AI 生成内容合规

本项目全部 AI 能力均经服务端调用 **DeepSeek**（`server.cjs:233`，`api.deepseek.com`）。

#### 微信类目审核层面

| 技术来源 | 需提交材料 |
|---|---|
| 自研 | 小程序主体自己的《互联网信息服务算法备案》（生成合成类）→ **仅企业法人可办** |
| **使用第三方**（本项目） | ① 技术方的算法备案证明<br>② 与技术方的合作协议（须含**算法名称 / 应用场景 / 备案编号**任一） |

→ **本项目无需自办算法备案**，只需向 DeepSeek 开放平台索取上述两份材料。可参考腾讯云、火山方舟的做法（控制台自助获取，约 10 天）。

> ✅ **关键前提已满足**：DeepSeek（深度求索）是境内已备案模型。
> ❌ **若当初选型为 Claude / GPT 系列，此路完全不通** —— 境外模型无境内算法备案，拿不出任何材料。

> ⚠️ **另一个坑**：若 DeepSeek 是通过华为云/腾讯云 MaaS **转售**接入的，平台方可能无法出具加盖技术主体公章的合作协议（要 DeepSeek 原厂章）。本项目直连 `api.deepseek.com`，与深度求索直接签约，位置更优。

#### 法规层面（与平台审核是两回事）

- 本主体在法律上被定性为"**生成式人工智能服务提供者**"，需承担内容安全主体责任；
- 通过 API 调用已备案大模型面向境内公众提供服务的，通常做 **大模型登记**即可（轻于算法备案）；
- 上线后须在**显著位置公示模型名称 + 登记号**；
- **AI 生成内容必须显著标识"AI 生成"字样** —— 覆盖范围：对话回复、`chatGrading` 评分、`pronunciationBank` 注音翻译。

### 4.3 服务器域名与 ICP 备案

小程序要求 `wx.request` 只能访问**已备案的 HTTPS 域名**，且须写入小程序后台白名单。

现状 `src/utils/api.js:5` 指向：

```
https://1421249792-l5mg9larpx.ap-nanjing.tencentscf.com
```

**此域名无法用于小程序** —— 腾讯云 SCF 默认域名仅限测试，不支持 ICP 备案，微信不认。

须完成：

1. 购买域名；
2. **ICP 备案**（纯等待 1–3 周，主体为学校/机构时更慢）—— **应立即启动**；
3. 配置 SSL 证书；
4. 通过 API 网关 / 云函数 URL 化 将 SCF 挂载到该域名；
5. 在小程序后台配置 `request` 合法域名。

> 开发阶段可在开发者工具勾选"不校验合法域名"绕过，**但体验版与正式版无法绕过**。

### 4.4 资源体积

`public/images/` 49 MB，远超小程序主包 2 MB 上限。

**处理方案**：

1. 11 张 PNG 全部压缩（目标单张 < 300 KB，WebP 优先）；
2. 上传腾讯云 COS / CDN，`<Image>` 走网络地址；
3. **注意**：小程序 CSS **禁止引用本地图片**，`background-image` 只能用网络地址或 base64；
4. 引入**分包加载**：主包放登录/主菜单，发音题库与文化游戏各占一个分包。

---

## 五、技术选型

### 5.1 框架：Taro

| 方案 | 评估 |
|---|---|
| **Taro 4**（推荐） | 保留 React + Hooks 语法，现有组件逻辑改动最小；同源代码可同时产出小程序与 H5 |
| uni-app | Vue 语法，全部页面需重写 |
| 原生小程序 | 全部重写，且失去与 Web 版共享代码的能力 |
| mpvue / wepy | 已停止维护 |

### 5.2 工程组织：新建独立工程，不就地改造

```
speakwise/                    ← 现有 Web 版（保持不动，继续收集数据）
├── src/                          React + Vite
└── 小程序化方案.md

speakwise-mp/                 ← 新建 Taro 工程
├── src/
│   ├── core/                     ← 共享层：从 Web 版原样复制/软链
│   │   ├── data/                 全部数据与题库
│   │   └── utils/                纯逻辑模块
│   ├── platform/                 ← 平台适配层（新增）
│   │   ├── storage.js            localStorage ↔ wx.setStorageSync
│   │   ├── request.js            fetch ↔ Taro.request
│   │   ├── recorder.js           Web Audio ↔ getRecorderManager
│   │   └── tts.js                speechSynthesis ↔ InnerAudioContext
│   └── pages/ components/        ← 按 Taro 写法移植
```

**理由**：现有 Web 版很可能正在承担数据收集职责，就地改造会中断研究。独立工程 + 平台适配层，既保住 Web 版，又让未来两端共享同一套业务逻辑。

**适配层是关键设计** —— 把 4 类平台差异收敛到 4 个文件里，`core/` 以下的业务代码完全不感知平台。

---

## 六、改造清单

### 6.1 原样复用（约 2800 行，零改动）

| 模块 | 行数 | 说明 |
|---|---|---|
| `src/data/*.js`（8 个） | ~200 | 纯数据，含 drills / scenarios / topics / studyManual / cultureGameCh1-3 / moduleMeta / constants |
| `src/data/pronunciationBank.json` | 552 题 | 128 KB，可直接进包 |
| `utils/pronunciationBank.js` | 462 | **纯逻辑，无 React 依赖**（作者已刻意如此设计），可直接复用 |
| `utils/conversationMetrics.js` | 255 | 纯计算 |
| `utils/transcript.js` | 115 | 纯转换 |
| `utils/chatGrading.js` | 33 | 业务规则 |
| `utils/topicRecommender.js` | 31 | 纯算法 |
| `utils/moduleBuilders.js` | 35 | 纯构造 |
| `utils/helpers.jsx` | 45 | 工具函数 |

> `pronunciationBank.js` 是最有价值的一块 —— 它的头部注释明确写了"选课、评测模式映射、讯飞响应解析全部放在这里，而不是埋在组件的事件回调里"，这个决策让它在迁移中完全免疫。

### 6.2 机械替换（量大、风险低）

| 项目 | 规模 | 改法 |
|---|---|---|
| `localStorage` | **35 处 / 9 文件**（`recordStore.js` 独占 19 处） | 统一走 `platform/storage.js` 适配层；`recordStore.js` 只需替换底层读写 |
| `fetch` | `utils/api.js` 一处封装 | 换 `Taro.request`。**`apiFetch` 的超时 / 401 清 token / 非 JSON 响应兜底逻辑可整体保留**，只需替换请求原语 |
| `react-router` | 24 条路由 | 换 Taro 路由 + `app.json` pages。`useNavigate` / `useParams` 签名基本一致 |
| JSX 标签 | 全量 | `<div>`→`<View>`、`<span>`/`<p>`→`<Text>`、`<img>`→`<Image>` |
| `document` / `window` | 14 处 / 7 文件 | 见 6.3 |
| CSS | 406 行 | 小程序不支持 `*` 选择器、标签选择器；组件间样式默认隔离，需逐个核对 |

### 6.3 需要重写

| 模块 | 行数 | 原因 | 工作量 |
|---|---|---|---|
| `utils/audioRecorder.js` | 110 | Web Audio API 不存在 | 小（mp3 直传方案下大幅简化） |
| `hooks/useSpeech.js` | 46 | `SpeechRecognition` + `speechSynthesis` 不存在 | 中（拆成 STT / TTS 两块 + 权限处理） |
| `hooks/useIsMobile.js` | — | 小程序无桌面/移动之分 | **直接删除** |
| `components/AbilityRadar.jsx` | 69 | **小程序不支持内联 `<svg>` 元素** | 中（改 `<canvas type="2d">`；`pt()` / `poly()` 极坐标计算可复用，仅渲染层重写） |
| `utils/exporters.js:154` | 166 | `document.createElement("a").download` 不存在 | 中（改 `wx.getFileSystemManager().writeFile` + `wx.shareFileMessage`；**"绝不导出 token"的约束须保留**） |
| `components/Onboarding.jsx` | 81 | `swiper` 库不适用 | 小（换小程序原生 `<Swiper>`） |
| `context/AuthContext.jsx` | 101 | 存储层替换 + 可选微信登录 | 小 |
| `context/AppContext.jsx` | 37 | 存储层替换 | 小 |

### 6.4 页面级清单（18 个）

| 页面 | 行数 | 难度 | 备注 |
|---|---|---|---|
| `Login.jsx` | 183 | 🟢 易 | 表单，账号密码可原样 |
| `HSKSelect.jsx` | 40 | 🟢 易 | |
| `MainMenu.jsx` | 238 | 🟢 易 | |
| `OralMenu.jsx` / `WrittenMenu.jsx` | — | 🟢 易 | 导航 |
| `SceneList.jsx` | — | 🟢 易 | |
| `CultureMenu.jsx` | — | 🟢 易 | |
| `StudyManual.jsx` | 82 | 🟢 易 | |
| `CultureGame.jsx` | 196 | 🟡 中 | 大图须走 CDN |
| `StudentRecords.jsx` | 414 | 🟡 中 | 导出功能重写 |
| `UserCenter.jsx` | 336 | 🟡 中 | 存储层 + 班级 |
| `TeacherDashboard.jsx` | 602 | 🟡 中 | 全项目最大页面；注意 `GET /api/tasks` 的 N+1 问题 |
| `ChatView.jsx` | 329 | 🟠 难 | 语音核心，草稿提交队列逻辑须保真 |
| `DrillView.jsx` | 373 | 🟠 难 | 语音核心 |
| `PronunciationMenu.jsx` | — | 🟡 中 | |
| `PronunciationDaily.jsx` | 156 | 🟡 中 | 依赖 `PronunciationDrill` |
| `PronunciationBank.jsx` | 216 | 🟡 中 | 自定义题库 + AI 注音 |
| `PronunciationTest.jsx` | 85 | 🟡 中 | 依赖 `PronunciationDrill` |
| `components/PronunciationDrill.jsx` | 323 | 🟠 难 | 录音 + 评测核心，四处语音调用点之一 |

---

## 七、后端改造

全部为**新增**，现有接口零改动：

| 优先级 | 项 | 说明 |
|---|---|---|
| P0 | **域名备案 + 网关接入** | 阻塞一切真机测试 |
| P0 | **讯飞 IAT 接口** | `POST /api/asr`，复用现有讯飞 WebSocket 客户端与凭证 |
| P0 | **讯飞 TTS 接口** | `POST /api/tts`，支持语速参数（慢速跟读演示） |
| P1 | 微信登录 | `POST /api/auth/wx-login`：`code2session` → openid → 绑定既有账号。**JWT 体系不变** |
| P2 | 导出接口 | `GET /api/records/export` 服务端生成文件，返回下载链接（规避小程序文件导出限制） |
| P2 | 大模型登记公示 | 前端展示模型名称 + 登记号 + "AI 生成"标识 |
| P3 | `GET /api/tasks` N+1 优化 | 现有注释已标记：逐 (任务 × 学生) COUNT，改为单条 GROUP BY |

> 微信登录可**延后甚至跳过** —— 账号密码登录在小程序中完全可用。

---

## 八、实施计划

### 阶段划分

| 阶段 | 内容 | 人日 | 依赖 |
|---|---|---|---|
| **P0 · 外部流程** | 个体户注册 → 小程序注册 → 域名备案 → SSL → 网关接入 → DeepSeek 材料索取 | — | **立即启动，纯等待** |
| **P1 · 骨架** | Taro 工程搭建、适配层（storage / request）、登录 + 主菜单跑通、CI | 5 | P0 域名 |
| **P2 · 静态页** | 8 个易级页面 + 路由对齐 | 6 | P1 |
| **P3 · 资源** | 49 MB 图片压缩 + COS 上传 + 分包配置 | 3 | — |
| **P4 · 语音链路** | 服务端 IAT/TTS → 录音适配 → `useSpeech` 重写 → 四个调用点接入 → 真机联调 | 8 | P1、P0 讯飞 |
| **P5 · 复杂页** | ChatView、DrillView、PronunciationDrill、TeacherDashboard、StudentRecords | 10 | P4 |
| **P6 · 收尾** | 雷达图 canvas、导出重写、合规标识、权限引导、提审 | 6 | P5 |
| | **合计** | **约 38 人日（单人 7–8 周）** | |

### 可并行项

- 图片压缩（P3）与代码工作无依赖，**可立刻开始**；
- 域名备案与全部代码工作无依赖，**应立刻开始**；
- DeepSeek 材料索取约 10 天，**应立刻开始**。

**关键路径是外部流程，不是编码。** 建议本周内同时启动上述三项。

### 建议的降级路线（科研演示场景）

若暂不打算正式发布：

- 注册**个人主体**，走**体验版 + 体验成员（上限 100 人）**，完全绕开类目审核与备案；
- STT/TTS 仍走服务端讯飞（个人主体下本来也只能如此）；
- 代码层面与正式版**差异极小**（主要在 STT 是否用插件）；
- 风险：AI 功能处于灰色地带，仅限不公开发布的演示场景。

---

## 九、风险登记

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| R1 | **WechatSI 插件对个人主体不可用**，且开发者工具不报错、提审才暴露 | 高 | 已识别；个体户主体 / 服务端 IAT |
| R2 | 域名备案周期不可控（1–3 周，学校主体更慢） | 高 | **立即启动**；开发期用"不校验合法域名" |
| R3 | 个体户能否作为算法备案主体，资料互相矛盾 | 中 | 走第三方技术路线绕开；被驳回则申企业主体 |
| R4 | 深度合成类目审核被驳回 | 高 | 参考社区经验：剥离 AI 生成类功能（如 AI 作图）常可通过；必要时先上不含 AI 的版本 |
| R5 | 49 MB 图片压缩后仍超包体限制 | 中 | 全量 CDN + 分包加载 |
| R6 | 小程序内联 SVG 不可用 | 中 | 能力雷达改 canvas，已识别 |
| R7 | mp3 直传影响讯飞评测精度 | 中 | 退回 PCM → 小程序侧拼 WAV 头方案 |
| R8 | 中小程序 API 域名备案与 Web 版域名冲突 | 低 | 可共用同一域名，仅路径区分 |
| R9 | **各地管局政策存在差异** | 中 | 落地前向当地通信管理局确认，不以本文档为最终依据 |

---

## 十、待决事项

1. **主体类型最终确认** —— 个人（演示）还是个体工商户（正式）？（决定 4.1.2 STT 方案）
2. **是否保留 Web 版长期并行** —— 若保留，须建立 `core/` 共享机制；若放弃，可考虑就地改造以减少重复代码。
3. **小程序是否要承接数据收集** —— 若 SRTP 研究数据需从小程序采集，`conversationMetrics` 的口径必须与 Web 版严格一致（该模块可原样复用，风险可控）。
4. **微信登录是否上线** —— 影响 P1 阶段工作量。

---

## 附：实施进度

### 已完成（P1 骨架 + P4 语音链路）

工程在 [`mp/`](mp/)，另有 [`mp/README.md`](mp/README.md) 讲怎么跑、怎么分层、怎么自检。

- **工程与适配层**：Taro 4.2.1 + React 18，webpack5，自定义导航栏；`platform/` 八个适配文件（storage / request / nav / recorder / asr / tts / privacy / file + export）收敛了全部平台差异
- **共享逻辑**：`core/` 从 Web 版同步（`npm run sync:core`，补丁失配即报错），另有 91 项纯逻辑自检（`npm run check:core`）
- **服务端**：`generateAuth` 参数化；新增 `POST /api/asr`（讯飞 IAT）与 `POST /api/tts`（讯飞 TTS，语速可调）；新增音频格式嗅探 + ID3 剥头。**现有接口零改动**
- **语音链路已用真实凭证端到端验证**：TTS 合成 → ASR 回读，识别结果与原文一致
- **页面**：15 个（登录 / HSK / 主菜单 / 口语 / 场景 / 对话 / 发音四页 / 练习 / 写作 / 记录 / 用户中心 / 手册 / 诊断）
- **体积**：主包 985 KB / 2048 KB

### 未完成

- **P0 外部流程**（域名备案、DeepSeek 材料索取）—— 与代码无关，需并行启动
- **教师面板**（602 行）—— 教师侧不使用语音，不阻塞语音链路
- **文化文游** + 49MB 插图压缩 / COS / 分包（P3）
- 微信登录、AI 生成内容合规标识与模型登记号公示
- `GET /api/tasks` 的 N+1 优化

### 与原方案的偏差

1. **STT 走服务端讯飞 IAT，不用 WechatSI 插件** —— 个人主体与个体户通用，且与 TTS 共用一条服务链路，只有一套代码路径
2. **工程放在 `speakwise/mp/` 而不是同级 `speakwise-mp/`** —— 同一仓库管理，共享 `core/` 也方便
3. **新增一页语音链路诊断页** —— 开发者工具在录音格式这件事上不可信，真机排查没有它只能靠猜



- [web-view 组件文档（微信官方）](https://developers.weixin.qq.com/miniprogram/dev/component/web-view.html)
- [小程序开放的服务类目（微信官方）](https://developers.weixin.qq.com/miniprogram/product/material/)
- [小程序备案服务内容选择指引（微信官方）](https://developers.weixin.qq.com/miniprogram/product/record/receord_category.html)
- [深度合成 · AI 问答类目资质要求（微信官方）](https://developers.weixin.qq.com/minigame/product/material/shenduhecheng/Aiwenda.html)
- [小程序深度合成审核资质 · 微信云开发（腾讯云）](https://cloud.tencent.cn/developer/article/2620669)
- [算法备案文档 · 腾讯云开发 CloudBase](https://docs.cloudbase.net/ai/release/algorithm-filing)
- [深度合成类目与个人主体讨论 · 微信开放社区](https://fuwu.weixin.qq.com/community/develop/doc/000aa49b1e0890d6ebe1be0ff6bc00)
- [一文解答：产品接入第三方 AI 大模型的合规义务（广悦律所）](https://www.wjngh.cn/jofacolumn/info.aspx?itemid=5201)
- [客户应用上架：算法备案资质申请流程（华为云）](https://support.huaweicloud.com/maas_faq/maas_faq_0013.html)
- ["算法备案"必备条件有哪些（腾讯云社区）](https://cloud.tencent.cn/developer/article/2555467)
